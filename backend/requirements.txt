fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
asyncpg==0.29.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
alembic==1.12.1
cryptography==42.0.5
PyJWT==2.8.0
prometheus-client==0.20.0
redis==5.0.4
slowapi==0.1.8
httpx==0.28.1
boto3==1.34.160


# We'll also need the shared library; we can install from local path or publish to pypi
# For now, we'll assume it's available as a package, or we can mount it.
# In development, we can install with pip install -e ../../shared

# Development dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
black==23.11.0
isort==5.12.0
mypy==1.7.0
flake8==6.1.0

# Shared library is installed separately in Dockerfiles or local setup scripts.
