global:
  resolve_timeout: 5m

route:
  receiver: "webhook"
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: "webhook"
    webhook_configs:
      - url: "http://siem-webhook.local/ingest"
        send_resolved: true
